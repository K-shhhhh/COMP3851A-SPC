# Backend runtime dependencies; installing a driver does not implement its integration.
fastapi
uvicorn[standard]
pydantic-settings
email-validator
python-multipart>=0.0.20,<1
celery[redis]>=5.4,<6
psycopg[binary]>=3.2,<4
SQLAlchemy>=2.0.52,<2.1
pgvector
redis>=5,<6
alembic

# Generate and validate signed JWT access tokens
PyJWT>=2.10,<3

# Secure password hashing using the recommended Argon2 algorithm
pwdlib[argon2]

# Rag Pipeline Requirements
python-dotenv
nudenet
openai
ollama
nltk
numpy
pandas
pymupdf
